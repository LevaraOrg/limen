# limen — the committed half of the context.
#
# The descriptor next to it (limen.yaml) is machine-local and gitignored: it
# says who you are here. This file says what the project is bound to, and is
# the same for everyone who clones the repository. Tools may read it, never
# derive identity from it — see meta.go.

# Inherited norms. An agent-plugins.org package, pinned to a version.
profiles: levara-baseline@1.1.0

# Where `limen profile sync` materialises them. Both have defaults
# (.claude/skills and docs/adr); only the deviation is written here.
adrTarget: docs/adr
